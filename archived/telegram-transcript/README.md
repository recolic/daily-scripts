# tg transcript

this is a very naive tool to record all telegram msg.

simpledb.py has a naive built-in query tool, to pull or count some db records

## Usage

run tg-transcript.py

please read `daemon.sh` for example usage
