#!/bin/bash

if [ $# -eq 0 ]; then
    echo "Usage: $0 RSEC_KEY1 RSEC_KEY2 ..."
    exit 1
fi

echo "# Generated by rsec_export. DO NOT modify"
for key in "$@"; do
    value=$(rsec "$key")
    echo "${key}:${value}"
done
